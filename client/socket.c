#include <stdio.h>
// Create a socket
// Determine server address & port number (from config file)
// Connect to server
// Write/Read data to connected socket

// socket()
// connect()
// write()
// read()

// Client establishes connection
void establish_connection(char* server_addr, char* server_port) {
    // get server addr from config file
    // get server port from config file
    
    printf("Server IP Address: %s\n", server_addr);
    printf("Server Port Number: %s\n", server_port);

}
