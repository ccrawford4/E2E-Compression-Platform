#include <stdio.h>
#include <stdlib.h>
// parse_json()

int main(int argc, char**argv) {
    if (argc != 2) {
        printf("usage: \n");
        printf("./compdetect_client <file_name.json>\n");
        exit(EXIT_FAILURE);
    }
   // char* server_address = parse_json("server_ip");
   // char* server_port = parse_json("UDP_dest_port_number");

   // establish_connect(server_address, server_port);

}
